{
  "entities": {
    "IdTemplate": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "IdTemplate",
      "type": "object",
      "description": "Represents a template for generating IDs, such as student, employee, or membership cards.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ID template."
        },
        "name": {
          "type": "string",
          "description": "Name of the ID template (e.g., 'Student ID', 'Employee Badge')."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the ID template and its purpose."
        },
        "fields": {
          "type": "array",
          "description": "List of required and optional fields for this template (e.g., name, address, date of birth).",
          "items": {
            "type": "string"
          }
        },
        "layout": {
          "type": "string",
          "description": "Defines the layout of the ID template using a data structure like JSON or string.",
          "format": "string"
        }
      },
      "required": [
        "id",
        "name",
        "description",
        "fields",
        "layout"
      ]
    },
    "GeneratedId": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "GeneratedId",
      "type": "object",
      "description": "Represents a generated ID for a user based on a selected template.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the generated ID."
        },
        "templateId": {
          "type": "string",
          "description": "Reference to IdTemplate. (Relationship: IdTemplate 1:N GeneratedId)"
        },
        "userId": {
          "type": "string",
          "description": "Reference to User. (Relationship: User 1:N GeneratedId)"
        },
        "creationDate": {
          "type": "string",
          "description": "Date and time when the ID was generated.",
          "format": "date-time"
        },
        "fieldValues": {
          "type": "string",
          "description": "A JSON string containing the values entered by the user for the template fields.",
          "format": "string"
        },
        "photoUrl": {
          "type": "string",
          "description": "URL of the uploaded photo for the ID.",
          "format": "uri"
        },
        "qrCodeData": {
          "type": "string",
          "description": "Data encoded in the QR code associated with the ID.",
          "format": "string"
        }
      },
      "required": [
        "id",
        "templateId",
        "userId",
        "creationDate",
        "fieldValues",
        "photoUrl",
        "qrCodeData"
      ]
    },
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the IDGenius application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        },
        "name": {
          "type": "string",
          "description": "Full name of the user."
        }
      },
      "required": [
        "id",
        "email",
        "name"
      ]
    },
    "EventReport": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "EventReport",
      "type": "object",
      "description": "Represents a user-submitted traffic event or incident.",
      "properties": {
        "location": {
          "type": "string",
          "description": "The geographical location of the event."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the event."
        },
        "severity": {
          "type": "string",
          "description": "The severity level of the event.",
          "enum": ["low", "medium", "high"]
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user who submitted the report."
        },
        "user": {
          "type": "string",
          "description": "The display name of the user who submitted the report."
        },
        "userAvatar": {
          "type": "string",
          "description": "URL to the avatar of the user who submitted the report."
        },
        "picture": {
          "type": "string",
          "description": "URL of an image related to the event, if provided.",
          "format": "uri"
        },
        "createdAt": {
          "type": "string",
          "description": "The timestamp when the event was created.",
          "format": "date-time"
        }
      },
      "required": ["location", "description", "severity", "userId", "user", "userAvatar", "createdAt"]
    },
    "Comment": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Comment",
      "type": "object",
      "description": "Represents a comment made by a user on an event report.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The ID of the user who wrote the comment."
        },
        "author": {
          "type": "string",
          "description": "The display name of the user who wrote the comment."
        },
        "avatar": {
          "type": "string",
          "description": "URL to the avatar of the user who wrote the comment."
        },
        "text": {
          "type": "string",
          "description": "The content of the comment."
        },
        "createdAt": {
          "type": "string",
          "description": "The timestamp when the comment was created.",
          "format": "date-time"
        }
      },
      "required": ["userId", "author", "avatar", "text", "createdAt"]
    },
    "Video": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Video",
      "type": "object",
      "description": "Represents a user-uploaded video.",
      "properties": {
        "title": {
          "type": "string"
        },
        "videoUrl": {
          "type": "string",
          "format": "uri"
        },
        "thumbnailUrl": {
          "type": "string",
          "format": "uri"
        },
        "userId": {
          "type": "string"
        },
        "user": {
          "type": "string"
        },
        "userAvatar": {
          "type": "string",
          "format": "uri"
        },
        "likes": {
          "type": "number"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "title",
        "videoUrl",
        "userId",
        "user",
        "createdAt",
        "likes"
      ]
    },
    "PushSubscription": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "PushSubscription",
      "type": "object",
      "description": "Represents a browser push subscription for a user.",
      "properties": {
        "endpoint": {
          "type": "string",
          "format": "uri"
        },
        "expirationTime": {
          "type": ["number", "null"]
        },
        "keys": {
          "type": "object",
          "properties": {
            "p256dh": {
              "type": "string"
            },
            "auth": {
              "type": "string"
            }
          },
          "required": ["p256dh", "auth"]
        }
      },
      "required": ["endpoint", "keys"]
    },
    "Logement": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Logement",
      "type": "object",
      "description": "Represents a short-term rental apartment.",
      "properties": {
        "title": {
          "type": "string",
          "description": "Title of the listing."
        },
        "description": {
          "type": "string",
          "description": "Detailed description of the apartment."
        },
        "address": {
          "type": "string",
          "description": "Full address of the property."
        },
        "pricePerNight": {
          "type": "number",
          "description": "Price per night in USD."
        },
        "imageUrls": {
          "type": "array",
          "description": "List of URLs for the apartment images.",
          "items": {
            "type": "string",
            "format": "uri"
          }
        },
        "amenities": {
          "type": "array",
          "description": "List of available amenities.",
          "items": {
            "type": "string"
          }
        },
        "ownerId": {
          "type": "string",
          "description": "The UID of the admin who added the listing."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of when the listing was created."
        }
      },
      "required": [
        "title",
        "address",
        "pricePerNight",
        "imageUrls",
        "ownerId",
        "createdAt",
        "description",
        "amenities"
      ]
    },
    "Annonce": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Annonce",
      "type": "object",
      "description": "Represents an official announcement for users.",
      "properties": {
        "title": {
          "type": "string",
          "description": "The title of the announcement."
        },
        "content": {
          "type": "string",
          "description": "The main content of the announcement."
        },
        "source": {
          "type": "string",
          "description": "The source of the announcement (e.g., 'Hôtel de Ville')."
        },
        "createdAt": {
          "type": "string",
          "description": "The timestamp when the announcement was created.",
          "format": "date-time"
        }
      },
      "required": [
        "title",
        "content",
        "source",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous",
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profile information.  User documents are accessible only to the user and admins (admin check not modeled here).",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/generatedIds/{generatedId}",
        "definition": {
          "entityName": "GeneratedId",
          "schema": {
            "$ref": "#/backend/entities/GeneratedId"
          },
          "description": "Stores generated ID information for each user. Organized as a subcollection of the user document to ensure that only the user can access these documents. Includes denormalized 'userId' for authorization independence.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            },
            {
              "name": "generatedId",
              "description": "The unique identifier for the generated ID."
            }
          ]
        }
      },
      {
        "path": "/idTemplates/{idTemplateId}",
        "definition": {
          "entityName": "IdTemplate",
          "schema": {
            "$ref": "#/backend/entities/IdTemplate"
          },
          "description": "Stores ID templates.  Templates are intended to be created and modified by admins (admin check not modeled here).",
          "params": [
            {
              "name": "idTemplateId",
              "description": "The unique identifier for the ID template."
            }
          ]
        }
      },
      {
        "path": "/events/{eventId}",
        "definition": {
          "entityName": "EventReport",
          "schema": {
            "$ref": "#/backend/entities/EventReport"
          },
          "description": "Stores user-submitted traffic event reports. These are public and can be read by anyone, but can only be created by authenticated users.",
          "params": [
            {
              "name": "eventId",
              "description": "The unique identifier for the event report."
            }
          ]
        }
      },
      {
        "path": "/events/{eventId}/comments/{commentId}",
        "definition": {
          "entityName": "Comment",
          "schema": {
            "$ref": "#/backend/entities/Comment"
          },
          "description": "Stores comments for a specific event report. Comments are publicly readable but can only be created by authenticated users.",
          "params": [
            {
              "name": "eventId",
              "description": "The unique identifier for the parent event report."
            },
            {
              "name": "commentId",
              "description": "The unique identifier for the comment."
            }
          ]
        }
      },
      {
        "path": "/videos/{videoId}",
        "definition": {
          "entityName": "Video",
          "schema": {
            "$ref": "#/backend/entities/Video"
          },
          "description": "Stores user-uploaded videos. Publicly readable, but only the owner can create/delete.",
          "params": [
            {
              "name": "videoId",
              "description": "The unique identifier for the video."
            }
          ]
        }
      },
      {
        "path": "/videos/{videoId}/comments/{commentId}",
        "definition": {
          "entityName": "Comment",
          "schema": {
            "$ref": "#/backend/entities/Comment"
          },
          "description": "Stores comments for a specific video. Comments are publicly readable but can only be created by authenticated users.",
          "params": [
            {
              "name": "videoId",
              "description": "The unique identifier for the parent video."
            },
            {
              "name": "commentId",
              "description": "The unique identifier for the comment."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/pushSubscriptions/{subscriptionId}",
        "definition": {
          "entityName": "PushSubscription",
          "schema": {
            "$ref": "#/backend/entities/PushSubscription"
          },
          "description": "Stores push notification subscriptions for a user. A user can have multiple subscriptions from different devices/browsers. Accessible only by the user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            },
            {
              "name": "subscriptionId",
              "description": "The unique identifier for the push subscription."
            }
          ]
        }
      },
      {
        "path": "/annonces/{annonceId}",
        "definition": {
          "entityName": "Annonce",
          "schema": {
            "$ref": "#/backend/entities/Annonce"
          },
          "description": "Stores official announcements. Publicly readable, but only admins can write.",
          "params": [
            {
              "name": "annonceId",
              "description": "The unique identifier for the announcement."
            }
          ]
        }
      },
      {
        "path": "/logements/{logementId}",
        "definition": {
          "entityName": "Logement",
          "schema": {
            "$ref": "#/backend/entities/Logement"
          },
          "description": "Stores short-term rental apartment listings. Publicly readable, but only admins can write.",
          "params": [
            {
              "name": "logementId",
              "description": "The unique identifier for the logement."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to support the IDGenius application and the new Kinshasa Flow traffic reporting features. It emphasizes a clear separation between private user data and public-facing community data. User-private data remains locked down, while the new `/events` collection is made public for reading to allow all users to benefit from traffic reports. Write access to `/events` and its `comments` subcollection is restricted to authenticated users (including anonymous ones) to prevent spam and ensure accountability, with rules enforcing that the `userId` field matches the creator's UID. A new `/annonces` collection has been added for official announcements, which is publicly readable and writable only by administrators. A `/logements` collection has also been added to store apartment listings, which are publicly readable and writable only by an admin."
  }
}
